import React from "react";

const Loading = () => {
  return (
    <div>
      <span className="flex items-center justify-center h-full text-lg text-black">
        ...
      </span>
    </div>
  );
};

export default Loading;
